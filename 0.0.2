#include <iostream>
#include <string>
#include <sstream>
using namespace std;

bool massive(int array[10])
{
    string string;
    getline( cin, string );
    istringstream stream( string );
    bool result = true;
    for( int i = 0; i < 10; ++i ) {
        if ( !(stream >> array[ i ]) ) {
            result = false;
            break;
        }
    }
    return result;
}
int decrease(int array[10]) {
    for(int i = 0; i < 10; i++) {
        if (!(array[i] >= array[i-1])) {cout << "The input sequence must be non-decreasing."; exit(0); }
    }
    return 0;
}

int main()
{
    int posl[10];
    int sum;
    if(massive(posl)) {
        decrease(posl);
        cin >> sum;
        bool no = true;
        for(int i=0; i<10; i++) {
            for (int j = 1; j<10; j++) {
                if ((posl[i]+posl[j] == sum) && (posl[i] != posl[j])) {cout << posl[i] << " " << posl[j] << endl; no = false; exit(0); }
            }
        }
             if(no) cout << "There is no such pair of numbers.";
         }

    else {
        cout<<"An error has occurred while reading the input sequence.";
    }

}
